# 现场测试说明（v0.6.9）

## 升级准备

1. 退出旧版程序、厂商上位机和串口工具，保持手机 APP 与待测传感器断开。备份旧版整个目录，将新版 ZIP 解压到独立目录，再复制旧版 `data` 文件夹以保留设备和历史。若复制旧版 `config`，请重新核对下面的设置。
2. 在采集设置中选择真实 EW-DTU02、实际 COM 端口及与网关一致的波特率。保持 `115200` 时，目标同时连接数与真实网关并发上限都建议设为 `3`，连接超时设为 `40` 秒，单次采集时长先设为 `3` 分钟。不要因登记了更多设备而把真实网关并发上限提高到设备总数。
3. 启用本次在场的设备。手动断开的设备会显示“已手动断开”，必须点击“重新连接”才会重新加入本次运行的队列；重启程序后按设备的长期启用设置恢复。

## 先用 5 台，再扩大到车间全部设备

1. 先启用 5 台，记录它们的 MAC 与安装位置。启动后确认最多 3 台在总览前面显示为已连接，并持续更新最近有效样本；其余设备显示排队位次。
2. 等当前批次每台都采满设定时长。程序应逐台断开当前批次，确认没有遗留连接后重新扫描。下一批优先选择尚未采集的设备，刚完成采集的设备应移到队尾。连接过渡时，前面可能短暂少于 3 台；不要把“连接中”误认成已采到数据。
3. 至少观察 3 次换批，并记录每次断开、扫描、连接、首条有效样本的时间。若某台连上后 20 秒仍无有效数据，软件会隔离该台并延后重试；其他设备不应被错误标成已采集。
4. 5 台验证后逐步增加到全部设备。建议连续运行至少两个完整轮次，确认每台都获得采集窗口，没有设备长期排在队尾。总览中的位次是当前调度顺序；扫描未发现或失败退避时，实际连接顺序可调整。

## 故障时保留证据

- 首页下载诊断 ZIP，记下电脑本地时间、出问题的设备 MAC、所在电机、操作和当时已连接数量。诊断 ZIP 包含配置、状态快照及滚动 RX/TX 日志；不要把现场日志或数据库上传到公开仓库。
- `TX AT+DISCON` 后，需看到对应 MAC 的 `+DISCON` 或重同步确认；单独的 `OK` 只说明指令被接收。`AT_RESPONSE_TIMEOUT` 表示回复未完整结束，程序应先用 `AT+CNNI=` 核对真实连接状态。
- 持续增加的 `non_ascii_bytes`、`bad_notify_count` 或串口 `Write timeout` 表明数据链路仍需排查。检查 RS485 A/B 接线、接地与屏蔽、转换器、电源、终端匹配和实际波特率。软件轮换改动不能代替这些现场检查。
- 不要仅凭“已连接”判断成功；以“采集中”和最近有效样本持续更新为准。数值保持相同可能是电机工况稳定，应检查样本时间而非只看数值。

本版默认三台并发依据现有复测记录。四台真正同时在线或更高波特率仍属单独实验，不作为本轮验收条件。
