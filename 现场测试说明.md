# 现场测试说明（v0.6.0）

## 默认设备

| 设备 | MAC 地址 | 位置 |
|---|---|---|
| CBF0-W31 | F8:C5:C0:B8:91:7E | 7-100 |
| CBF1-W31 | C2:37:21:02:DE:EF | 7-100 |
| CBF2-W31 | E3:58:B1:4B:81:B5 | 7-100 |
| CBF3-W31 | E9:14:70:05:2E:F9 | 7-100 |
| CBF4-W31 | D5:E4:4E:23:86:0B | 7-100 |
| CBF5-W31 | F8:0D:11:C2:A5:2E | 7-100 |
| CBF6-W31 | F6:D0:D8:17:D9:76 | 7-100 |

## 先做单设备对照

1. 退出旧程序、厂家上位机和串口调试工具，确认传感器已从厂家软件及手机断开。用户此前已确认过断开，此步骤用于复测时保持相同条件。
2. 新版解压到独立目录。没有硬件时可以先用模拟模式熟悉界面和日志，但该结果不能证明现场蓝牙正常。
3. 现场接上网关，在“采集设置”选择真实串口、实际 COM 号、网关波特率，连接超时设为 40 秒，同时连接数先设为 1。
4. 暂时停用其他设备，仅启用一台曾被厂家软件验证过的设备，先在便于观察的位置测试，再恢复到原安装位置。停用不会删除历史记录。
5. 等待约 5 分钟，观察是否依次出现网关回复、RSSI、蓝牙连接和有效数据。无论成功或失败，都下载一次诊断日志，并记录实际测试时间、设备名称和位置。
6. 单设备稳定后，逐台启用，先测 2 台，再提高到默认 5 台同时连接。7 台会轮询，不会同时全连。每次改变数量后单独保存日志，方便比较。

## 如何判断

| 现象 | 含义与下一步 |
|---|---|
| 串口未打开 | 查看日志的串口打开错误；确认端口号和其他程序占用情况。 |
| 串口打开但没有网关回复 | 不等于网关通信正常；检查波特率、接线及网关是否在指令模式。不要直接恢复出厂。 |
| 有 RSSI，但未连接 | 接收到广播只说明发现设备；需要查看 TX 连接参数及紧随其后的 RX 错误。 |
| `AT_ERROR` | 网关拒绝了当前指令，可能是参数、固件支持或当前状态问题；日志可看到被拒绝的完整指令。 |
| `CNN_BUSY` | 网关明确报告连接忙；保持当前方案稍后重试，日志用于确认当时的指令顺序。 |
| `TIMEOUT` | 网关明确报告蓝牙连接超时；日志记录设备、地址和方案，不能仅凭此认定传感器坏了。 |
| `AT_RESPONSE_TIMEOUT` | 没有收到完整指令响应，程序暂停指令队列；先下载日志，再点击“按已保存设置重新连接网关”。 |
| `SERVICE_NOT_FOUND` / `CCCD_ERROR` | 服务发现或通知订阅失败，需要结合传感器协议和原始回复继续适配。 |
| 已连接，等待有效数据 | 蓝牙连接成功，但还未解析到有效温振数据，或最近 10 秒无新有效样本；日志中找 `+NOTIFY` 和 `First valid sensor sample`。 |
| 采集中并显示温度、振动值 | 最近 10 秒内有有效温振样本；应继续观察稳定性和历史曲线。 |

## 失败时请保留

首页点击“下载诊断日志”，发送整个 ZIP；同时说明测试时间、COM 号、单设备还是多设备、哪些设备在原安装位置、是否重启过网关。ZIP 包含版本、采集设置、运行状态和滚动原始日志，不含历史数据库。

本地日志在 EXE 同目录的 `logs\monitor.log`。文件会轮转，失败后尽快保存，不要等数天后再取。不要把运行日志或现场数据库提交到公开仓库。
