# 现场测试说明（v0.6.7）

## 升级与双设备验证

1. 退出旧程序、厂商上位机和串口调试工具，保持手机 APP 与传感器断开。
2. 备份旧版目录，将 v0.6.7 Windows ZIP 解压至独立目录；复制旧版 data 和 config 文件夹以保留登记设备、阈值、历史和串口设置。
3. 在采集设置选择真实 EW-DTU02、实际 COM 端口和网关波特率，连接超时建议 40 秒，同时连接数设为 2。
4. 只启用本次在场的两台传感器。两台供电正常、靠近已安装天线的网关。只改变同时连接数不会停用其他登记设备。
5. 等待发现与依次建链，首页应显示两台“采集中”，两台最近样本时间持续更新。数值不变可能是传感器静止，不应仅凭温度/振动数值相同判断没有更新。
6. 在两台之间多次切换“优先连接并实时监控”。当两台已经连接时，切换优先目标应保留两路采集。
7. 将同时连接数改为 1 后测试来回优先切换；目标未在当前候选批次时，应先断开、扫描发现，再连接目标。设置恢复为 2 后应补齐第二路。
8. 关闭软件后重新打开，观察已有两路连接能否被接管。保存一份诊断 ZIP；再在实际安装位置和更长运行时间下验证。

## 日志与结果

- 扫描、连接、有效样本是不同阶段。日志中的 CONNECT 只是排队请求，TX AT+CONN 表示已发送，匹配的 +CONN 表示建链结果，First valid sensor sample 表示已解析有效温振数据。
- 正常批次为扫描发现、AT+SCAN=0 后通过 AT+SCAN? 回读确认停止、多个 AT+CONN 逐一完成、持续 +NOTIFY。采集中不应重复开启扫描。
- 声明长度或格式错误的通知会被丢弃并单独计数；不把损坏数据写成零，不把坏通知当作 BLE 连接失败。
- TIMEOUT 是网关明确返回的连接超时，会按设备重试；AT_RESPONSE_TIMEOUT 是回复未完整结束，有限恢复后仍失败会暂停队列，并显示首个阻断原因。
- 优先目标未被发现时会重新发现；存在正常设备时，已发现的失败设备可在空闲通道重试；需要新一轮扫描时，等待当前批次获得完整采集时间。新增或启用设备会触发一次及时发现。满通道的已连接优先设备会保持优先，直到解除优先或租约到期。
- 非 ASCII 字节、坏通知计数和指令重试仍可能增长；它们反映串口链路异常，不能仅凭软件侧日志确定唯一硬件责任层。

失败时先点击首页“下载诊断日志”，记录测试时间、设备、位置和操作，再到采集设置点击“按已保存设置重新连接网关”。日志包包含配置、状态快照、计数与滚动 RX/TX 时间线；不要将现场诊断包或数据库提交到公开仓库。无需为了本版验证恢复出厂设置。

## 新增设备验证

先让原两台持续采集，再登记并启用第三、第四台，同时连接数设为 4 或 5。观察每台依次完成建链并持续输出至少 5 秒有效数据，再建立下一台。无数据设备应在 20 秒观察窗口后单独释放并显示重试原因，其他正常设备不应因空闲通道而每 30 秒全部断开。

只收到连续的 20 字节帧头不算有效温振样本。新版请求 MTU247 并拒绝把相邻截短帧拼成数据；判断成功需同时满足“采集中”、完整样本持续更新以及没有异常断开。运行中登记已连接设备后，检查状态能否同步，不能仅凭最近通知判断采集成功。

断开指令已被接收但迟迟未完成时，新版会等待最多 15 秒。收到匹配的断开结果后应继续调度，不能把第一次 OK 当作实际断开，也不能在五秒时重复发送导致误锁。重开软件时检查四台状态与最新样本能否接管。
