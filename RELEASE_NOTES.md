# v0.6.7 — 新增设备与迟到断开处理

v0.6.6 的现场日志显示：新增设备已报告 BLE 连接却没有有效通知，程序仍在两秒后连接下一台；扫描阶段开始的补位计时又会立即断开刚建好的连接。另一次断开操作先收到 OK，程序五秒后重发，随后因重复操作 ERROR 锁住队列，但真正的断开通知在六秒后才到。本版针对这些已确认的软件缺陷修复：

- 每台建链后观察至少 5 秒有效温振数据，再继续添加下一台。20 秒无有效数据则只释放异常连接，延后重试并轮换连接兼容参数，不主动清空正常连接。
- 移除可能产生 20 字节截短通知的 MTU23 连接组合，统一请求 MTU247。连续截短帧不会再被拼成伪造样本，真正的分片续包仍可重组。
- 新登记设备如果已经存在于网关确认的连接表中，会同步为已连接，避免有通知却一直排队并阻止后续调度。
- 成功连接不再立即清零失败次数；收到有效数据才确认成功。已有连接列表的重复通知不会清空正在更新的样本。
- 本轮已发现的失败设备利用空闲通道重试，避免反复清空正常连接来扫描；新注册设备获得一次及时发现，之后的轮换从实际建链后计算采集时间。
- 断开独立等待 15 秒，已收到指令确认时不重复发送。必须收到匹配 MAC 的断开事件和结束标记，不能用单独 OK 或另一台的断开作为成功证据。
- 多路连接查询收集完整查询窗口，验证每条连接记录的结束标记；第一台的 OK 不会提前启动后续指令。
- 停扫后读取 `AT+SCAN?`，必须收到完整的停扫状态和结束标记才连接设备；若仍在扫描则有限重试。乱码后的孤立 OK 不再能单独作为停扫成功的证据。
- 保留旧数据与设置兼容，保留损坏通知隔离和原始诊断记录。

验证：96 项自动化测试及 Windows 可执行程序启动、模拟采集自检通过。现场两台新增传感器分别经电脑蓝牙连续读取 30 秒，获得 297 / 298 条通知；通过原网关也分别取得完整数据，但增加连接时仍出现掉线、串口乱码和连接指令未完整回复，尚未通过四台同时持续采集验收。本版修复已确认的软件问题，不代表剩余链路问题已经解决。

软件改动针对调度与事务处理；传感器未广播、无数据输出、射频连接失败或串口物理损坏仍需根据诊断区分。发布包不包含现场日志或数据库，不更改传感器采样参数，不自动恢复出厂设置。

升级时退出旧程序，备份并将 data、config 文件夹复制到新版独立目录。只启用本次实际在场的设备。同时连接数是上限，“已连接，等待数据”不代表已经采集成功。
