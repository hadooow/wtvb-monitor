# v0.6.3 — 优先使用从 v0.4 EXE 核实的连接参数

静态检查用户提供的 v0.4 发布 EXE，确认连接时同时使用扫描地址、MTU247 和配对扩展参数。v0.6.2 缺少这个完整组合，且此前日志在普通连接阶段已经暂停。

- 首选新增 v0.4 配对/扫描地址方案；无扫描地址时与 v0.4 一样使用自动地址。
- 保留之前五个兼容方案、串口指令队列、日志、MAC 编辑和默认设备。
- 增加实际原传感器扫描回复对应的完整连接指令回归测试。
- 仓库 docs/v04-comparison.md 记录 EXE 指纹、检查方法、参数差异及已知限制。

下载 WTVB-Monitor-v0.6.3-windows-x64.zip，全部解压后运行 WTVB-Monitor.exe，无需 Python。退出新旧程序后可复制旧版 data 文件夹保留历史。

当前没有硬件无需立即测试。下次测试先只启用 WTVB01-BT50，将连接数设为 1，确认 COM 号和波特率。首次连接日志应包含 AT+CONN=FE6DF407B3E4,0,1,247,40000,1,40,20,0,600,1,1,0。测试后下载诊断日志。

此版恢复已核实的连接参数组合，并非完整回退 v0.4。v0.4 在车间也失败；真实 BLE 和现场稳定性仍待验证。网关迟到回复导致队列暂停的问题仍保留为已知限制，本版没有修改等待策略。
