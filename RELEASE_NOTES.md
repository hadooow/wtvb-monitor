# v0.6.5 — 按复测结果分离扫描与持续采集

2026-09-20 现场复测确认：同一只 WTV 在 BLE 连接和物理位置不变时，扫描开启阶段可复现损坏通知和非 ASCII 字节；关闭扫描后 627 条通知全部通过声明长度及 0x55 0x61 帧头检查；再次开启扫描又出现损坏。该结果说明扫描广播与传感器通知并行回传与损坏现象高度相关，但仍不能仅凭软件侧证据确定具体硬件责任层。

本版据此调整串口采集流程：

- 串口模式改为“扫描发现 → 确认 AT+SCAN=0 → 连接/持续采集”。连接成功后保持停扫，不再像 v0.6.4 那样在连接事务结束后自动执行 AT+SCAN=1。
- 连接失败或当前设备断开后，才重新开启扫描，为下一次发现/重试/轮换准备。
- 调度器只有在网关扫描确实开启且目标最近 10 秒被发现时才允许发起串口连接；停扫采集阶段不会拿旧的 last_seen 继续排队连接第二台。
- 保留 v0.4 已验证的连接参数组合、40 秒连接超时、5 秒写库间隔和现有配置兼容。
- +NOTIFY 增加声明长度检查，长度不一致直接拒收并计数，不把破损报文送入 WTVB 帧解析器。
- 诊断状态增加扫描状态、活动连接数、坏通知计数、非 ASCII 字节计数和首个阻断性错误；原始 RX/TX 时间线继续保留。
- 保持通知/扫描产生的 OK 与当前 AT 命令事务终止标记分开处理，避免异步流量直接完成错误的命令事务。
- 新增回归测试覆盖停扫后连接、采集期保持停扫、失败后恢复扫描、坏通知长度拒收和扫描阶段连接资格。

复测还表明第二只 E9:5B:EA:0C:2D:E1 在完整重启后恢复广播、连接和稳定通知；原 WTV FE:6D:F4:07:B3:E4 靠近网关天线后也成功连接。这些现象支持可发现状态和射频链路参与此前失败，不能据此认定必须更换 USB 转换器、网关、天线或传感器。

本版的软件改动目标是避免已复现的“扫描 + 通知并行回传”场景，并加强坏报文拒收和诊断证据。它不宣称已经定位 RS485、电平转换、USB 驱动或网关固件中的最终责任层，也不保证已经解决此前 Windows ReadFile 异常。
