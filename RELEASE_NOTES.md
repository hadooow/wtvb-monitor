# v0.6.4 — 修复现场断开通知与活跃连接查询

现场 v0.6.3 已成功连接 WTVB01-BT50 并解析数据，但有两个可重现的软件兼容问题：

- 网关发送 +DISCON:2,0,FE6DF407B3E4,8，旧解析器只支持 handle,mac 格式，遗漏了实际断开。新版兼容 role,handle,mac,reason 和旧格式，正确更新连接数、清除半帧缓存并记录原因码。
- 重开串口后，网关可能仅回复 +CNB:1，同时继续发送已连接设备的通知。新版仅在查询窗口内收到唯一 MAC/handle 的非空通知载荷时确认这一连接并继续；无通知、数量冲突、断开或错误回复不被当作成功。
- 断开其他设备的通知不会结束当前连接请求。
- 保留 v0.4 参数组合、默认设备、MAC 编辑及日志。

下载 WTVB-Monitor-v0.6.4-windows-x64.zip，全部解压后运行 EXE，无需 Python。退出新旧程序后可复制旧版 data 目录保留历史。

建议下一次只启用 CBF2 或 CBF4 一台，停用包括 WTV 在内的其余设备，同时连接数设为 1，网关断电重启后测试并导出日志。单台成功后再验证 WTV 与 CBF 双设备。

本版不宣称解决 CBF 建链失败或网关在连接请求后停止回复的原因；连接指令超时仍会暂停队列。实际 BLE 稳定性需现场复测。用户提供的语雀开发文档未能读取全文，需要 PDF 核对固件/GATT/配对要求；没有修改传感器采样参数。
