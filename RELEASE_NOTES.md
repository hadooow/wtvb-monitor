# v0.6.6 — 多设备同时采集与优先切换修复

v0.6.5 连上第一台后关闭扫描，但调度器仍要求扫描开启才能连接第二台；优先切换也先检查同一条件，导致目标一直排队。本版基于已发布的 v0.6.5，合入此前未发布方案中的批次发现、停扫采集和坏通知隔离，并补齐实测暴露的问题。

- 同一轮扫描收集多个候选，停扫后逐个建立连接，再同时接收各设备数据。即使第一台建链耗时超过 10 秒，第二台仍可继续连接。
- 已连接的两台之间切换优先设备不触发断开。优先目标未发现时主动进入重新发现流程；缺少设备时限制重建频率，不无限反复打断正常采集。
- 支持启动时接管多路旧连接：对于仅返回 CNB:N 及实时通知的固件，必须有数量一致、MAC 与连接句柄均不冲突的有效通知证据；矛盾或不足的回复仍视为失败。
- 在完整接收行后的串口空闲窗口发送指令，减少与高频通知碰撞的机会。仅对停扫/启扫、查询、按 MAC 断开做最多两次重发，保持同一事务接收回复；断开仍必须收到匹配结果，连接指令不盲目重发。
- 校验通知类型、长度、地址及句柄；坏通知单独计数并清理对应的半帧，保留其他设备采集和首个真正阻断错误。保留 v0.6.5 的非 ASCII 字节统计，新增指令重试计数。
- 保留原有设备配置、历史数据库兼容、连接参数及发布包结构。

验证：79 项自动测试通过；Windows x64 本地打包及 EXE 模拟采集、首页和诊断下载自检通过。2026-09-20 使用原 EW-DTU02、USB-RS485 转换器及两只实物传感器，完成约 2.5 分钟的组合测试：两路同时持续采集、四次双设备优先切换、两路降一路及两次单路目标切换、恢复两路、重开网关连接后接管两路均通过。测试期间发布了两台分别 335 和 459 个有效样本更新；这不是长时间稳定性测试。期间有一次连接列表查询丢失回复，经两次有限重发恢复。

现场测试仍观察到少量串口损坏通知。本版修复调度逻辑并加强异常恢复，不宣称消除了网关、RS485、转换器或 USB 驱动层的全部异常。超出有限重试仍无完整回复时，请先下载诊断日志，再在采集设置中重新连接网关。

升级请退出旧程序，将新版解压到独立目录，备份并复制旧版 data 和 config 文件夹；不要同时启动两个版本。先启用实际在场的两台设备，同时连接数设为 2。下载 Assets 中的 Windows ZIP，不能将 Source code 当作可执行程序。
