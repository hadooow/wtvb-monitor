# v0.6.0 — 网关连接诊断与 Win11 发布改进

## 修改

- 按 EW-DTU02 网关测试手册调整默认连接操作超时至 40 秒。
- 优先普通 10 字段连接指令；扫描地址、MTU23、配对扩展作为后续兼容方案。
- 串行处理 AT 指令，连接前停止扫描，等连接回复及结束标记；保留跨串口超时的半行数据。
- 裸 ERROR 关联当前设备；回复未完整结束时暂停队列，提供重新连接入口。
- 避免连接/断开并发操作，失败设备让出下一次尝试机会。
- 新增 CBF1 默认 MAC C2:37:21:02:DE:EF，兼容旧数据库升级。
- 自动保存滚动诊断日志，首页一键下载；分开显示串口、网关响应、蓝牙连接与采集状态。
- Windows x64 便携 ZIP 包含 Python 环境、静态资源和现场默认配置；构建时执行测试和 EXE 模拟采集自检。

## 下载与运行

下载 Assets 中 `WTVB-Monitor-v0.6.0-windows-x64.zip`，全部解压后双击文件夹内的 `WTVB-Monitor.exe`。无需单独安装 Python。采集设置中确认实际 COM 号、波特率和 40 秒连接超时。不要只复制 EXE，也不要从压缩包里直接运行。

日志：点击首页“下载诊断日志”，或打开 EXE 同目录 `logs/monitor.log`。旧版数据迁移方法和现场复测步骤见包内 README 与《现场测试说明》。

## 验证范围

自动测试和 EXE 模拟自检验证软件流程，不验证真实蓝牙射频连接。本次没有现场网关和传感器，也未能读取传感器语雀协议页；现有测量帧解析保持不变。实际现场连通性与多设备稳定性仍需携带日志复测，本版本不宣称已确认或排除全部现场故障原因。
