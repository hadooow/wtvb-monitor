# v0.6.1 — 修复网关空连接列表导致的启动停止

现场日志确认 EW-DTU02-M 固件 V1.5(2507081320) 正常回复扫描停止和版本查询，但对 AT+CNNI= 返回 +CNB:0 后没有追加 OK。v0.6.0 将其误判为超时并暂停队列，导致开启扫描和连接传感器的指令都未发送。这是上版程序引入的兼容回归，不能由此判定网关硬件损坏。

## 修复

- 仅对 AT+CNNI= 的明确空列表 +CNB:0 兼容没有尾随 OK 的回复。
- 保留原有约 5 秒查询等待窗口，接收可能追加的 OK；等待结束后继续发送扫描指令。
- 无回复、非空或不完整列表、其他命令的超时仍会被报告。
- 增加现场回复重放、可选 OK、错误和超时边界的回归测试。
- 保留 CBF1 默认登记、持久诊断日志与全部 v0.6 功能。

## 下载运行

下载 WTVB-Monitor-v0.6.1-windows-x64.zip，全部解压到新目录，退出旧程序后双击 WTVB-Monitor.exe。无需安装 Python。若需保留历史记录，先退出新旧程序，再把旧版 data 文件夹复制到新版 EXE 同目录。

采集设置中确认真实网关、实际 COM 号、波特率和 40 秒连接超时。启动后日志应继续出现 TX AT+SCAN=1。若仍无法采集，点击首页“下载诊断日志”，发送新版 ZIP。

该补丁解决已从日志确认的启动阻塞。实际蓝牙连接和多设备采集稳定性仍需要现场复测；Windows 打包自检使用模拟采集，不代替硬件测试。
