# v0.6.2 — 修复连接前停止扫描阻塞，支持编辑 MAC

最新现场日志中，WTVB01-BT50（FE:6D:F4:07:B3:E4）广播 RSSI 为 -46/-47 dBm。程序排队连接后只发送 AT+SCAN=0，5 秒后响应超时，实际 AT+CONN 未发送。此现象不能直接判定为网关或传感器故障。

- 取消连接前强制停止扫描，连接请求直接入队；保持连接结果和终止回复匹配检查。
- 由网关工作线程统一恢复扫描，移除调度器的重复扫描指令。
- 默认加入 WTVB01-BT50：FE:6D:F4:07:B3:E4。
- CBF0 更正为 E8:C5:C0:B8:91:7E，旧数据库自动迁移并保留历史；如果两个地址都存在，保留历史并停用旧 F8 条目。
- 每台设备的“设置”可查看和编辑 MAC，并启用/停用采集。校验地址格式及重复地址，保留设备 ID、历史和阈值；活跃连接必须先停用并等待结束后改地址。
- 保留 v0.6.1 空连接列表兼容修复和滚动诊断日志。

下载 WTVB-Monitor-v0.6.2-windows-x64.zip，全部解压到新目录，退出旧程序后运行 WTVB-Monitor.exe，无需 Python。保留整个文件夹及 _internal。需要旧历史时，退出新旧程序后复制旧版 data 文件夹到新版 EXE 同目录。

建议先只启用 WTVB01-BT50，同时连接数设为 1，确认真实串口、COM 号、波特率及 40 秒连接超时。首页“下载诊断日志”可导出 ZIP；本地日志为 EXE 同目录 logs/monitor.log。请记录测试时间并保存成功或失败日志。

自动测试覆盖扫描广播中连接发出、回复匹配、地址升级保留历史、MAC 编辑和错误校验；Windows 打包后执行模拟采集及页面/诊断下载自检。真实 BLE 连接和车间多设备稳定性尚需现场复测，未取得独立 v0.4 源码，不能宣称已完全还原 v0.4 行为。
